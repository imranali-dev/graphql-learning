import { typeDefs } from "./typedef.user";
import { queries } from "./quries";
import { mutations } from "./mutation";
import { resolvers } from "./resolver.user";

export const Twwets = { typeDefs, queries, mutations, resolvers };